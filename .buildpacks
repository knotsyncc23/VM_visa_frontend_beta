# Force Coolify to use Dockerfile
# This file tells Coolify to skip Nixpacks auto-detection

# Disable Nixpacks
export NIXPACKS_NO_DEFAULT_CACHE_MOUNT=1
export DISABLE_NIXPACKS=true

# Use custom Dockerfile
export DOCKER_BUILDPACK=1